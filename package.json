{
  "name": "stata-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Stata do file management and execution",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "stata-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "clean": "rimraf dist",
    "prepare": "npm run build"
  },
  "keywords": ["mcp", "stata", "statistics", "sociology", "research"],
  "author": "Sociology Research Team",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}